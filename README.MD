Simple implementation of Binary Trees using javascript, there's still much to do.
It includes tests to ensure everything is working.

Install: cd BinaryTreee && npm install && node Main.js or npm test

